(videoai is a directory, and there's no indication it contains build artifacts, dependencies, or temporary files that should be ignored. It appears to be a regular project directory being modified.)

(empty)